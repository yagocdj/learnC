#include <stdio.h>

int main() {
    
    int i = 0, limit = 10;

    while (i <= limit)
        i++;
    
    int random_value;

    // fazendo o casting da divisao (temos uma divisao inteira)

    random_value = 10 / 7;
    printf("%d\n", random_value);

    return 0;
}
